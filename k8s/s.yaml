apiVersion: v1
kind: Service
metadata:
  name: webapp
  labels:
    name: containers-my-app
  namespace: default
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: webapp
  selector:
    name: containers-my-app
